{
  "name": "genwoxie",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "tsc -p tsconfig.json",
    "test": "npm run build && node --experimental-strip-types --test tests/extract.test.ts",
    "e2e": "node --experimental-strip-types e2e/smoke.ts && node --experimental-strip-types e2e/test-input.ts"
  },
  "devDependencies": {
    "@types/node": "^22",
    "playwright": "^1",
    "typescript": "^5"
  }
}
